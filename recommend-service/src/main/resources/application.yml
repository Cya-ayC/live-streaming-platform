spring:
  application:
    name: recommend-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: production
    sentinel:
      transport:
        dashboard: localhost:8858
        port: 8719
  redis:
    host: localhost
    port: 6379
    database: 5

server:
  port: 8087